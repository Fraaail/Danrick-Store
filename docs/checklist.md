# Project Checklist

This checklist highlights ongoing maintenance items and ensures the location preview feature remains up‑to‑date.

- [x] Google Maps iframe present in `App.tsx` with exportable constants
- [x] Location constants include embed‑friendly URL
- [x] `index.html` contains Open Graph and geo meta tags
- [x] Tests added for both constants and HTML meta tags
- [x] Documentation created (`docs/location.md`)
- [x] README updated to mention preview metadata
- [x] Add or update `og-image.png` if using a static map screenshot

> ✅ When moving store, follow steps in `docs/location.md` and then mark off the relevant items above.
